# Core runs on the Python 3.9+ standard library alone.
# Everything below is OPTIONAL and enhances the platform when present.

requests>=2.28      # faster/nicer HTTP (falls back to urllib if absent)
PyYAML>=6.0         # richer profile/policy YAML (built-in fallback parser otherwise)

# --- dev / test (optional) ---
pytest>=7.0
